
data = [
    {
    type: "person",
    firstname: "Psyche",
    lastname: "Rubena",
    age: 21,
    status: "Active",
    sex: "Female",
    avatar: "2-f.jpeg"
    },
    {
    type: "place",
    name: "Pott Shrigley",
    region: "England",
    avatar: "England.png",
    longitude: "53.309",
    latitude: "-2.084"
    },
    {
    type: "quote",
    title: "The Two Towers",
    description: "There is some good in this world, and it’s worth fighting for.",
    author: "J.R.R. Tolkien",
    avatar: "j-r-r-tolkien.jpg"
    },
    {
    type: "person",
    firstname: "Heriberto",
    lastname: "Othman",
    age: 41,
    status: "Active",
    sex: "Male",
    avatar: "1-m.jpeg"
    },
    {
    type: "place",
    name: "Valle de Banderas",
    region: "Mexico",
    avatar: "Mexico.png",
    longitude: "20.8",
    latitude: "-105.233333"
    },
    {
    type: "quote",
    title: "The Little Prince",
    description: "It is only with the heart that one can see rightly; what is essential is invisible to the eye.",
    author: "Antoine de Saint-Exupéry",
    avatar: "Antoine-de-Saint-Exupery.jpg"
    },
    {
    type: "person",
    firstname: "Jarlath",
    lastname: "Katarina",
    age: 37,
    status: "Active",
    sex: "Male",
    avatar: "3-m.jpeg"
    },
    {
    type: "place",
    name: "Ingwiller",
    region: "France",
    avatar: "France.png",
    longitude: "48.87",
    latitude: "7.48"
    },
    {
    type: "quote",
    title: "Jane Eyre",
    description: "I am no bird; and no net ensnares me: I am a free human being with an independent will, which I now exert to leave you.",
    author: "Charlotte Brontë",
    avatar: "charlotte-bronte.jpg"
    },
    {
    type: "person",
    firstname: "Telmo",
    lastname: "Emmy",
    age: 42,
    status: "Active",
    sex: "Male",
    avatar: "4-m.jpeg"
    },
    {
    type: "place",
    name: "Paulsstadt",
    region: "Germany",
    avatar: "Germany.png",
    longitude: "53.632756",
    latitude: "11.404299"
    },
    {
    type: "quote",
    title: "A Tale of Two Cities",
    description: "It was the best of times, it was the worst of times, it was the age of wisdom, it was the age of foolishness, it was the epoch of belief, it was the epoch of incredulity, it was the season of Light, it was the season of Darkness, it was the spring of hope, it was the winter of despair.",
    author: "Charles Dickens",
    avatar: "Charles-Dickens.jpg"
    },
    {
    type: "person",
    firstname: "Osberht",
    lastname: "Fearghas",
    age: 55,
    status: "Active",
    sex: "Male",
    avatar: "12-m.jpeg"
    },
    {
    type: "place",
    name: "Wanderup",
    region: "Germany",
    avatar: "Germany.png",
    longitude: "54.666667",
    latitude: "9.316667"
    },
    {
    type: "quote",
    title: "The Bell Jar",
    description: "I took a deep breath and listened to the old brag of my heart: I am, I am, I am.",
    author: "Sylvia Plath",
    avatar: "sylvia-plath.jpg"
    },
    {
    type: "person",
    firstname: "Aegle",
    lastname: "Galaad",
    age: 31,
    status: "Active",
    sex: "Female",
    avatar: "5-f.jpeg"
    },
    {
    type: "place",
    name: "Yasinovataya",
    region: "Ukraine",
    avatar: "Ukraine.png",
    longitude: "48.127778",
    latitude: "37.8625"
    },
    {
    type: "quote",
    title: "Beloved",
    description: "Love is or it ain’t. Thin love ain’t love at all.",
    author: "Toni Morrison",
    avatar: "toni-morrison.jpg"
    },
    {
    type: "person",
    firstname: "Gautbert",
    lastname: "Anais",
    age: 27,
    status: "Active",
    sex: "Male",
    avatar: "14-m.jpeg"
    },
    {
    type: "place",
    name: "Lamstedt",
    region: "Germany",
    avatar: "Germany.png",
    longitude: "53.637778",
    latitude: "9.096667"
    },
    {
    type: "quote",
    title: "The Perks of Being a Wallflower",
    description: "We accept the love we think we deserve.",
    author: "Stephen Chbosky",
    avatar: "stephen-chbosky.jpg"
    },
    {
    type: "person",
    firstname: "Kamila",
    lastname: "Despoina",
    age: 54,
    status: "Active",
    sex: "Female",
    avatar: "6-f.jpeg"
    },
    {
    type: "place",
    name: "Danghara",
    region: "Tajikistan",
    avatar: "Tajikistan.png",
    longitude: "38.098333",
    latitude: "69.346667"
    },
    {
    type: "quote",
    title: "The Great Gatsby",
    description: "And so we beat on, boats against the current, borne back ceaselessly into the past.",
    author: "F. Scott Fitzgerald",
    avatar: "f-scott-fitzgerald.jpg"
    },
    {
    type: "person",
    firstname: "Bonaventure",
    lastname: "Terenti",
    age: 38,
    status: "Active",
    sex: "Female",
    avatar: "7-f.jpeg"
    },
    {
    type: "place",
    name: "Santa Severina",
    region: "Italy",
    avatar: "Italy.png",
    longitude: "39.15",
    latitude: "16.916667"
    },
    {
    type: "quote",
    title: "Velveteen Rabbit",
    description: "Generally, by the time you are Real, most of your hair has been loved off, and your eyes drop out and you get loose in the joints and very shabby. But these things don’t matter at all, because once you are Real you can’t be ugly, except to people who don’t understand.",
    author: "Margery Williams",
    avatar: "Margery-Williams.jpg"
    },
    {
    type: "person",
    firstname: "Alcina",
    lastname: "Donna",
    age: 43,
    status: "Active",
    sex: "Female",
    avatar: "8-f.jpeg"
    },
    {
    type: "place",
    name: "Spring Valley",
    region: "United States",
    avatar: "United-States.png",
    longitude: "36.1125",
    latitude: "-115.250278"
    },
    {
    type: "quote",
    title: "The Grapes of Wrath",
    description: "Ever’body’s askin’ that. ‘What we comin’ to?’ Seems to me we don’t never come to nothin’. Always on the way.",
    author: "John Steinbeck",
    avatar: "john-steinbeck.jpg"
    },
    {
    type: "person",
    firstname: "Caroline",
    lastname: "Viktoras",
    age: 36,
    status: "Active",
    sex: "Female",
    avatar: "9-f.jpeg"
    },
    {
    type: "place",
    name: "Midway",
    region: "United States",
    avatar: "United-States.png",
    longitude: "30.494878",
    latitude: "-84.46259"
    },
    {
    type: "quote",
    title: "Wuthering Heights",
    description: "Whatever our souls are made of, his and mine are the same.",
    author: "Emily Brontë",
    avatar: "emily-bronte.jpg"
    },
    {
    type: "person",
    firstname: "Kazimiera",
    lastname: "Yafa",
    age: 39,
    status: "Active",
    sex: "Female",
    avatar: "10-f.jpeg"
    },
    {
    type: "place",
    name: "Macerata",
    region: "Italy",
    avatar: "Italy.png",
    longitude: "43.300278",
    latitude: "13.453333"
    },
    {
    type: "quote",
    title: "Their Eyes Were Watching God",
    description: "There are years that ask questions and years that answer.",
    author: "Zora Neale Hurston",
    avatar: "zora-neale-hurston.jpg"
    },
    {
    type: "person",
    firstname: "Asmundr",
    lastname: "Kenneth",
    age: 21,
    status: "Active",
    sex: "Male",
    avatar: "15-m.jpeg"
    },
    {
    type: "place",
    name: "Villa del Rey",
    region: "Spain",
    avatar: "Spain.png",
    longitude: "39.65",
    latitude: "-6.816667"
    },
    {
    type: "quote",
    title: "Little Women",
    description: "I am not afraid of storms, for I am learning how to sail my ship.",
    author: "Louisa May Alcott",
    avatar: "louisa-may-alcott.jpg"
    },
    {
    type: "person",
    firstname: "Prabhu",
    lastname: "Agnes",
    age: 26,
    status: "Active",
    sex: "Female",
    avatar: "11-f.jpeg"
    },
    {
    type: "place",
    name: "Cirueña",
    region: "Spain",
    avatar: "Spain.png",
    longitude: "42.411111",
    latitude: "-2.895833"
    },
    {
    type: "quote",
    title: "Anna Karenina",
    description: "All happy families are alike; each unhappy family is unhappy in its own way.",
    author: "Leo Tolstoy",
    avatar: "leotolstoy.jpg"
    },
    {
    type: "person",
    firstname: "Sanya",
    lastname: "Rodrigo",
    age: 26,
    status: "Active",
    sex: "Female",
    avatar: "13-f.jpeg"
    },
    {
    type: "place",
    name: "Ostrowy",
    region: "Poland",
    avatar: "Poland.png",
    latitude: "52.303611",
    longitude: "19.161667"
    },
    {
    type: "quote",
    title: "Kafka on the Shore",
    description: "Memories warm you up from the inside. But they also tear you apart.",
    author: "Haruki Murakami",
    avatar: "haruki-murakami.jpg"
    },
    {
    type: "person",
    firstname: "Aibinn",
    lastname: "Tansy",
    age: 30,
    status: "Active",
    sex: "Male",
    avatar: "16-m.jpeg"
    },
    {
    type: "place",
    name: "Berbesti",
    region: "Romania",
    avatar: "Romania.png",
    longitude: "44.988056",
    latitude: "23.868333"
    },
    {
    type: "quote",
    title: "Les Misérables",
    description: "It is nothing to die; it is dreadful not to live.",
    author: "Victor Hugo",
    avatar: "Victor-Hugo.jpg"
    },
    {
    type: "person",
    firstname: "Felicius",
    lastname: "Ramchandra",
    age: 38,
    status: "Active",
    sex: "Male",
    avatar: "17-m.jpeg"
    },
    {
    type: "place",
    name: "Sales Oliveira",
    region: "Brazil",
    avatar: "Brazil.png",
    longitude: "-20.771944",
    latitude: "-47.838056"
    },
    {
    type: "quote",
    title: "Nineteen Eighty-Four",
    description: "Who controls the past controls the future. Who controls the present controls the past.",
    author: "George Orwell",
    avatar: "george-orwell.jpg"
    },
    {
    type: "person",
    firstname: "Anielka",
    lastname: "Sébastienne",
    age: 47,
    status: "Active",
    sex: "Female",
    avatar: "19-f.jpeg"
    },
    {
    type: "place",
    name: "Dunaivtsi",
    region: "Ukraine",
    avatar: "Ukraine.png",
    longitude: "48.889444",
    latitude: "26.856944"
    },
    {
    type: "quote",
    title: "Invisible Man",
    description: "Life is to be lived, not controlled; and humanity is won by continuing to play in face of certain defeat.",
    author: "Ralph Ellison",
    avatar: "ralph-ellison.jpg"
    },
    {
    type: "person",
    firstname: "Floriano",
    lastname: "Abaddon",
    age: 44,
    status: "Active",
    sex: "Male",
    avatar: "18-m.jpeg"
    },
    {
    type: "place",
    name: "Kishapu",
    region: "Tanzania",
    avatar: "Tanzania.png",
    longitude: "-3.731",
    latitude: "33.8"
    },
    {
    type: "quote",
    title: "Rebecca",
    description: "Last night I dreamt I went to Manderley again.",
    author: "Daphne du Maurier",
    avatar: "daphne-du-maurier.jpg"
    },
    {
    type: "person",
    firstname: "Tsubame",
    lastname: "Imani",
    age: 24,
    status: "Active",
    sex: "Male",
    avatar: "20-m.jpeg"
    },
    {
    type: "place",
    name: "Buddenstedt",
    region: "Germany",
    avatar: "Germany.png",
    longitude: "52.166667",
    latitude: "11"
    },
    {
    type: "quote",
    title: "Frankenstein",
    description: "Beware; for I am fearless, and therefore powerful.",
    author: "Mary Shelley",
    avatar: "mary-shelley.jpg"
    }
    ]

// calling main
main()
function handleExitButtonClick(){
    const container = document.getElementsByClassName('container')[0];
    
    // clearing the current html code within the container
    container.innerHTML = '';
    // creating upper box
    const upperBox = document.createElement('div');
    upperBox.className = 'upper-box';
    
    // creating search box
    const searchBox = document.createElement('div')
    searchBox.className = 'search-box';
    upperBox.appendChild(searchBox);

    // creating search container
    const searchContainer = document.createElement('div');
    searchContainer.className = 'search-cont';
    searchBox.appendChild(searchContainer);

    // creating the search bar
    const button = document.createElement('button');
    button.className = 'search-button';
    const img = document.createElement('img');
    img.src = './assets/search-icon-2048x2048-cmujl7en.png'
    img.alt = 'search-img';
    img.id = 'search-img';
    button.appendChild(img);

    const input = document.createElement('input');
    input.type = 'text';
    input.placeholder = 'Search';
    input.id = 'search-input';
    
    // adding searchbar to the container
    searchContainer.appendChild(button);
    searchContainer.appendChild(input);

    // creating search results container 
    const searchResultsContainer = document.createElement('div');
    searchResultsContainer.className = 'search-results-cont';
    searchBox.appendChild(searchResultsContainer);
    
    const searchResults = document.createElement('ul');
    searchResults.id = 'search-results';
    searchResultsContainer.appendChild(searchResults);

    container.appendChild(upperBox);
    main()
}

function handleButtonClick(item, truncatedString, button_icon){
    // Getting the upper box div
    const upperBox = document.getElementsByClassName("upper-box")[0];
    const container = document.getElementsByClassName("container")[0];
    // Clearing the div of all the elements nested within it
    upperBox.innerHTML = '';

    // creating the title bubble element
    const titleDiv = document.createElement("div");
    titleDiv.className = 'title-bubble'
    const title = document.createElement("p");
    title.className = 'title-bubble-txt';
    title.textContent = truncatedString;

    // creating exit button element
    const exitButton = document.createElement("button");
    const exitButtonImg = document.createElement("img");
    exitButtonImg.id = 'exit-button-img';
    exitButton.id = 'exit-button';
    exitButtonImg.src = 'assets/cross-23.png';
    exitButton.appendChild(exitButtonImg);
    exitButton.onclick = () => {
        handleExitButtonClick();
    }

    // creating icon
    const icon = document.createElement('img');
    icon.id = 'icon';
    icon.src = 'assets/' + button_icon;

    titleDiv.appendChild(icon);
    titleDiv.appendChild(title);
    titleDiv.appendChild(exitButton);
    
    // adding the div to the upper box div
    upperBox.appendChild(titleDiv);

    // creating the information about the item clicked
    infoContainer = document.createElement('div');
    infoContainer.className = 'info-container';

    imgContainer = document.createElement('div');
    infoSubContainer = document.createElement('div');
    imgContainer.className = 'info-img-container';
    infoSubContainer.className = 'info-sub-container';

    // adding the image to the image container
    img = document.createElement('img');
    img.id = 'info-img'
    img.src = 'assets/' + button_icon.substring(0, 4) + '-128.png';
    imgContainer.appendChild(img);

    for (let key in item){
        // skipping over type and avatar as they don't need to be displayed
        if (key == 'type' || key == 'avatar'){
            continue;
        }
        // creating the html elements and adding the corresponding text
        const attribute = document.createElement('p');
        attribute.className = 'info-item-attribute';
        const value = document.createElement('p');
        value.className = 'info-item-value';
        attribute.textContent = key.toUpperCase();
        console.log(item[key])
        value.textContent = item[key].toString().toUpperCase();
        infoSubContainer.appendChild(attribute);
        infoSubContainer.appendChild(value);
    }

    // nesting the divs previously created
    infoContainer.appendChild(imgContainer);
    infoContainer.appendChild(infoSubContainer);
    container.appendChild(infoContainer);

    

}

function displaySearchResultsAndHandleClick(query){
    const searchResultsContainer = document.getElementsByClassName('search-results-cont')[0];
    console.log("running")
    searchResultsContainer.innerHTML = ''; // Clear previous search results
    if (query.trim() === '') {
        // If search query is empty, do not display anything
        return;
    }
    const filteredData = data.filter(item => {
        // since the type of the data determines what we search by, we handle each case separately
        if (item.type == 'person'){
            var val = item.firstname.toLowerCase().includes(query.toLowerCase()) || item.lastname.toLowerCase().includes(query.toLowerCase());
            return val;
        }
        else if (item.type == 'place'){
            return item.name.toLowerCase().includes(query.toLowerCase());
        }
        else if (item.type == 'quote'){
            return item.description.toLowerCase().includes(query.toLowerCase());
        }
        else{
            return false;
        }
    });
    // if we have no results then return null
    if (filteredData.length == 0){
        return;
    }

    // Adding the Results string and a horizontal line
    const resultsString = document.createElement('p');
    resultsString.textContent = 'Results';
    resultsString.id = 'results-str'
    searchResultsContainer.appendChild(resultsString);

    const horizontalRule = document.createElement('hr');
    horizontalRule.id = 'hr-results';
    searchResultsContainer.appendChild(horizontalRule);

    // since we originally set the innerHTML of the container to '', we need to readd the list
    const searchResults = document.createElement('ul');
    searchResults.style.paddingLeft = '0';
    searchResults.style.listStyleType = 'none';
    var counter = 0; // counter to only display limited results in the event that there are too many

    filteredData.forEach(item => {
        counter++;
        var button_icon;
        if (counter > 17){
            searchResultsContainer.appendChild(searchResults);
        }
        else{
            // creating the list elements and the button elements 
            const listItem = document.createElement('li');
            const button = document.createElement('button');
            button.className = 'search-result-btn';
            listItem.className = 'search-result'

            listItem.appendChild(button);
            // we check if we need to truncate the string (then build the string), and add the appropriate icon for each type of data
            if (item.type == 'person'){
                button_icon = 'user.png';
                const fullName = `${item.firstname} ${item.lastname}`;
                listItem.style.background = "url('assets/user.png') no-repeat left center";
                listItem.style.backgroundSize = 'contain';
                var beforeMatch;
                var match;
                var afterMatch;
                const startIndex = fullName.toLowerCase().indexOf(query.toLowerCase())
                // if the name is too big to display then we need to truncate it.
                if (fullName.length > 20){
                    // if the start index is greater than 10, then we can display a truncated name from both sides
                    
                    if (startIndex > 10){
                        beforeMatch = fullName.substring(startIndex - 10, startIndex)
                        match = fullName.substring(startIndex, startIndex + query.length)
                        afterMatch = fullName.substring(startIndex + query.length, startIndex + 10 + match.length)
                        // span to add the search highlights
                        button.innerHTML = '...' + beforeMatch + '<span class="highlight">' + match + '</span>' + afterMatch + '...';
                    } else {
                        beforeMatch = fullName.substring(0, startIndex)
                        match = fullName.substring(startIndex, startIndex + query.length)
                        afterMatch = fullName.substring(startIndex + query.length, startIndex + 10 + match.length)
                        button.innerHTML = beforeMatch + '<span class="highlight">' + match + '</span>' + afterMatch + '...';
                    }
                    
                }
                else {
                    beforeMatch = fullName.substring(0, startIndex)
                    match = fullName.substring(startIndex, startIndex + query.length)
                    afterMatch = fullName.substring(startIndex + query.length, fullName.length)
                    button.innerHTML = beforeMatch + '<span class="highlight">' + match + '</span>' + afterMatch;
                }
                // add the item to the search results
                searchResults.appendChild(listItem);
            }

            else if (item.type == 'place'){
                button_icon = 'star.png';
                const fullName = `${item.name}`;
                listItem.style.background = "url('assets/star.png') no-repeat left center";
                listItem.style.backgroundSize = 'contain';
                var beforeMatch;
                var match;
                var afterMatch;
                const startIndex = fullName.toLowerCase().indexOf(query.toLowerCase())
                // if the name is too big to display then we need to truncate it.
                if (fullName.length > 20){
                    
                    // if the start index is greater than 10, then we can display a truncated name from both sides
                    
                    if (startIndex > 10){
                        beforeMatch = fullName.substring(startIndex - 10, startIndex)
                        match = fullName.substring(startIndex, startIndex + query.length)
                        afterMatch = fullName.substring(startIndex + query.length, startIndex + match.length + 10)
                        button.innerHTML = '...' + beforeMatch + '<span class="highlight">' + match + '</span>' + afterMatch + '...';
                    } else {
                        beforeMatch = fullName.substring(0, startIndex)
                        match = fullName.substring(startIndex, startIndex + query.length)
                        afterMatch = fullName.substring(startIndex + query.length, startIndex + 10 + match.length)
                        button.innerHTML = beforeMatch + '<span class="highlight">' + match + '</span>' + afterMatch + '...';
                    }
                }

                else{ 
                    beforeMatch = fullName.substring(0, startIndex)
                    match = fullName.substring(startIndex, startIndex + query.length)
                    afterMatch = fullName.substring(startIndex + query.length, fullName.length)
                    button.innerHTML = beforeMatch + '<span class="highlight">' + match + '</span>' + afterMatch;
                }
                searchResults.appendChild(listItem);
            }

            else if (item.type == 'quote'){
                button_icon = 'list.png';
                const fullName = `${item.description}`;
                listItem.style.background = "url('assets/list.png') no-repeat left center";
                listItem.style.backgroundSize = 'contain';
                var beforeMatch;
                var match;
                var afterMatch;
                const startIndex = fullName.toLowerCase().indexOf(query.toLowerCase());
                // if the name is too big to display then we need to truncate it.
                if (fullName.length > 20){
                    
                    // if the start index is greater than 10, then we can display a truncated name from both sides
                    ;
                    if (startIndex > 10){
                        beforeMatch = fullName.substring(startIndex - 10, startIndex)
                        match = fullName.substring(startIndex, startIndex + query.length)
                        afterMatch = fullName.substring(startIndex + query.length, startIndex + 10 + match.length)
                        button.innerHTML = '...' + beforeMatch + '<span class="highlight">' + match + '</span>' + afterMatch + '...';
                    } else {
                        beforeMatch = fullName.substring(0, startIndex)
                        match = fullName.substring(startIndex, startIndex + query.length)
                        afterMatch = fullName.substring(startIndex + query.length, startIndex + 10 + match.length)
                        button.innerHTML = beforeMatch + '<span class="highlight">' + match + '</span>' + afterMatch + '...';
                    }
                }
                else {
                    beforeMatch = fullName.substring(0, startIndex)
                    match = fullName.substring(startIndex, startIndex + query.length)
                    afterMatch = fullName.substring(startIndex + query.length, fullName.length)
                    button.innerHTML = beforeMatch + '<span class="highlight">' + match + '</span>' + afterMatch;
                }

                searchResults.appendChild(listItem);
            }
            const truncatedString = beforeMatch + match + afterMatch;
            // event listener for button click
            button.addEventListener('click', function(){
                handleButtonClick(item, truncatedString, button_icon);
            })

    }
    });
    // adding the results to the list
    searchResultsContainer.appendChild(searchResults);


}

// main function that is called on render
function main(){
        // Search box resizing
        const input = document.getElementById("search-input");
        const searchButton = document.getElementsByClassName('search-button')[0];
        const upperDiv = document.getElementsByClassName("upper-box")[0];
        const searchDiv = document.getElementsByClassName('search-box')[0];
        const searchImg = document.getElementById("search-img");
        searchToggled = false;
        input.addEventListener('focus', function() {
            
    
            if (!searchToggled){
                upperDiv.style.transition = 'height 0.3s ease';
                searchToggled = true;
                upperDiv.style.height = '2.75rem';
                searchDiv.style.margin='1.25rem 1.25rem 0';
                searchDiv.style.padding = '0.75rem 16.813rem 0.75rem 0.875rem';
                input.style.width = '18em';
                searchImg.src = 'assets/cross-23.png';
                
            }
        })
        searchButton.addEventListener('click', function(){
            if (searchToggled){
                searchToggled = false;
                upperDiv.style.transition = 'height 0.3s ease';
                upperDiv.style.height = '20rem'
                searchDiv.style.margin = '18.5rem 11.75rem 0 1.25rem';
                searchDiv.style.padding = '0.75rem 3.125rem 0.75rem 0.875rem';
                input.style.width = '5rem';
                searchImg.src = 'assets/search-icon-2048x2048-cmujl7en.png'
            }
        })
    
        // Search Filtering
        input.addEventListener('input', function() {
    
            const query = input.value.toLowerCase();
            displaySearchResultsAndHandleClick(query);
        })
    
    }
    
